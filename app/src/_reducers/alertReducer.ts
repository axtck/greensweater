import { Constants } from "../constants";

export const alert = (state: string, action: RAction) => {
    switch (action.type) {
        case Constants.alertConstants.SUCCESS:
            return {
            };
    }
};